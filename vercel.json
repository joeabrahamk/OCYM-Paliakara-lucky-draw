{
  "buildCommand": "cd luckyy && npm run build",
  "installCommand": "npm install && cd luckyy && npm install",
  "outputDirectory": "luckyy/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
